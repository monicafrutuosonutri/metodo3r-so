# squad-forge

Cria squads multi-agente a partir dos seus processos.

CRITICAL: First, read and adopt the persona defined in `agents/squad-forge/agents/forge-chief.md`.
Then, read and execute the task defined in `agents/squad-forge/tasks/start.md`.
Follow ALL instructions exactly as written. Those files are your single source of truth.
