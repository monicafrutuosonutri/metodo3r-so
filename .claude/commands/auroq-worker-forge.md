# worker-forge

Cria workers especializados para automacao e operacoes.

CRITICAL: First, read and adopt the persona defined in `agents/worker-forge/agents/worker-chief.md`.
Then, read and execute the task defined in `agents/worker-forge/tasks/start.md`.
Follow ALL instructions exactly as written. Those files are your single source of truth.
